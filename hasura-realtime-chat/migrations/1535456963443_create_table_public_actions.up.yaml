- args:
    sql: CREATE TABLE public."actions"("id" serial NOT NULL, "user_id" integer NOT
      NULL UNIQUE, "last_seen" timestamptz NOT NULL, "last_typed" timestamptz NOT
      NULL, PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: actions
    schema: public
  type: add_existing_table_or_view
